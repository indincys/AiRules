# AiRules

Merged AI rule providers generated from the current Voyager Surge profile.

- `Surge`: Surge external `RULE-SET` file. Use with `extended-matching,no-resolve`.
- `Clash`: Clash Verge / Mihomo `behavior: classical` provider payload.
